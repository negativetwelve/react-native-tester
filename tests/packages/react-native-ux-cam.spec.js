// Libraries
import {NativeModules} from 'react-native';


/* eslint-disable no-undef */
describe('react-native-ux-cam', () => {
  describe('RNUXCam', () => {
    it('should exist', () => {
      expect(NativeModules.RNUXCam).toBeDefined();
    });
  });
});
